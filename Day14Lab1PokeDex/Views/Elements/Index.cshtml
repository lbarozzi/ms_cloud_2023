@model IEnumerable<Day14Lab1PokeDex.Models.Element>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ElementName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ElementName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ElementID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ElementID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ElementID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
